name: openai
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - openai>=1.30.0
    - pydantic>=2.0
    - python-dotenv>=1.0
    - pillow>=10.0
    - pandas>=2.0
    - pyyaml>=6.0
